# Workstation

* Workstation is a scripting file which launch tmux sesion and the ssh private keys
* Locate `./git-pull.bash` file in the path `~/projects/` and adding those repositories that you keep require here.


