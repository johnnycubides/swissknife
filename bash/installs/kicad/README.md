Kicad funciona en x11, para wayland no hay soporte adecuado.

Instala `./kicad-install.bash` ejecuta `kicad-x11` para darle soporte
para x11

Para kicad-cli desde una AppImage modificar el siguiente script y pegar en /usr/local/bin/

```bash
./kicadX-cli.bash
```
